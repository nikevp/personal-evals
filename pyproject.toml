[project]
name = "logistics-cookbook"
version = "0.1.0"
description = "Harbor evaluations for real-world logistics tasks"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["harbor"]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.pytest.ini_options]
pythonpath = ["."]
